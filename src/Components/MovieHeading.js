import React from 'react'

const MovieHeading = ( {heading} ) => {
    return ( 
        <div className="col ms-3">
            <h1>{heading}</h1>
        </div>
    );
}

export default MovieHeading;